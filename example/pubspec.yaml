name: example
description: Example for iphone_has_notch package

version: 1.0.0

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  iphone_has_notch:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

